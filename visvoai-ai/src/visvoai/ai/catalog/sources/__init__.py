"""
catalog.sources — concrete `CatalogSource` implementations.

One file per source: `modelsdev` (live facts from models.dev), `remote` (cached
fetch). The baked floor lives in the engine as `BakedSource`.
"""
