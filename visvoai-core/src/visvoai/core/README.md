# Source map

Orientation for this tree lives in the `AGENTS.md` files — the package root's
[`AGENTS.md`](../../../AGENTS.md) maps every module (purpose, key files,
conventions, gotchas), and deeper directories carry their own. They're kept in
sync with the code and double as context for AI coding tools.
